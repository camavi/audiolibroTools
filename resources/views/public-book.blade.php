<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>{{ $book->name }}</title><style>body{margin:0;background:#090f1d;color:#edf2ff;font-family:Inter,system-ui,sans-serif}.page{max-width:960px;margin:auto;padding:48px 24px}.hero{display:grid;gap:22px;padding:42px;border:1px solid #314972;border-radius:22px;background:linear-gradient(135deg,#152b62,#111a31)}.eyebrow{color:#9cc0ff;font-size:.75rem;font-weight:700;letter-spacing:.1em;text-transform:uppercase}h1{margin:0;font-size:clamp(2rem,6vw,4rem)}p{color:#c3cee2;line-height:1.65}.actions{display:flex;flex-wrap:wrap;gap:12px;margin-top:12px}a{display:inline-flex;padding:12px 16px;border:1px solid #5e91ef;border-radius:10px;color:#fff;background:#2e6ee8;font-weight:700;text-decoration:none}a.secondary{background:#1c2941;border-color:#415a83}</style></head><body><main class="page"><section class="hero"><span class="eyebrow">Published book</span><h1>{{ $book->name }}</h1>@if($book->description)<p>{{ $book->description }}</p>@endif<div class="actions">@if($publication?->epub_file_path)<a href="{{ route('public.books.download',['keyBook'=>$book->key_book,'publication'=>$publication,'format'=>'epub']) }}{{ request()->getQueryString() ? '?'.request()->getQueryString() : '' }}">Download ePub</a>@endif @if($publication?->pdf_file_path)<a class="secondary" href="{{ route('public.books.download',['keyBook'=>$book->key_book,'publication'=>$publication,'format'=>'pdf']) }}{{ request()->getQueryString() ? '?'.request()->getQueryString() : '' }}">Download PDF</a>@endif @if($audio)<a class="secondary" href="{{ route('public.audiobooks.show',['keyBook'=>$book->key_book,'release'=>$audio]) }}{{ request()->getQueryString() ? '?'.request()->getQueryString() : '' }}">Listen audiobook</a>@endif</div></section></main></body></html>
